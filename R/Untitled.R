students <- c("tommy", "joe", "tanya", "yuxi", "julian", "mauricio")

sample(students, size = 2, replace = FALSE)

# Endosymbioesis, not so important
# traits don't match genotype trees
# high recombination \rightarrow more mutations

o <- c("tommy", "yuxi", "julian", "mauricio")
sample(o, size = 4, replace = FALSE)
